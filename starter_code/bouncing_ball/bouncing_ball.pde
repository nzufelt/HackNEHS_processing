final int displayWidth = 1200;
final int displayHeight = 1000;

// set the initial properties of your sketch
void setup() {
  size(displayWidth, displayHeight);  // this should be the first line in setup!
  background(255); 
   
}

// `draw` gets called every frame.  Think of the stuff in here as happening "each tick"
void draw() {
  background(255); // redraw the background 
}